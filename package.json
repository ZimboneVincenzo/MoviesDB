{
  "name": "movies",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "watch:sass": "sass sass/main.scss css/style.css -w",
    "start": "npm-run-all --parallel dev watch:sass"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "sass": "^1.35.2",
    "vite": "^2.5.2"
  },
  "dependencies": {
    "bootstrap": "^5.1.0"
  }
}
